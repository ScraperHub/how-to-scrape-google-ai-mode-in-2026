# Copy to .env and fill in from https://crawlbase.com (never commit .env).

# Normal (regular) token — lighter pages without full JS rendering.
CRAWLBASE_TOKEN=

# JavaScript token — use for Google AI surfaces and heavy client-side UI.
CRAWLBASE_JS_TOKEN=
